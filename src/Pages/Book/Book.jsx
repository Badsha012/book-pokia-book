import React, {} from 'react';

const Book = () => {
    // const data=use(bookPrimise);
    // console.log(data);
    return (
        <div>
            <h1>Single book</h1>
        </div>
    );
};

export default Book;